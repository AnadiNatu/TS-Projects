// Literal types allow you to specify a value that can be only be one specific literal value . This
// means that a variable with a literal type can only have one specific value and no other 



// String Literal Type

// let color : "red" | "blue" | "green";

// color = "red"; // valid code;

// color = "yellow " // error - Type '"yellow "' is not assignable to type '"red" | "blue" | "green"


// let numbers = 1|2|3|4;

// numbers = 5;
// console.log(numbers);